# v8_3/播放/__init__.py
"""视频播放子系统（V8_3 新增，**播放已换成 V2 自研内核**）。

播放本身在 :mod:`wangpan.player`（ctypes 直连 libav*，画面由 Qt 控件自绘）；
这里只剩两块**与内核无关**的支撑能力：

* :mod:`媒体信息`  —— ffprobe 探测分辨率/码率/音字幕轨（只读元数据，不参与播放）
* :mod:`直链探测`  —— 实测 TTFB / 带宽 / Range 支持，供 AI 选起播参数
* :mod:`语音识别`  —— 本地 Whisper（独立 venv + 常驻工作进程）

原来的 ``vlc绑定`` / ``播放核心`` / ``播放出口`` / ``显示环境`` / ``游离窗口``
已随整合删除：那 5 个模块存在的唯一理由就是"把画布交给外部播放器"。
"""
