# 识别评测报告

> 由 `工具/识别评测.py` 生成，**不要手改**（改标注集或解析层后重跑）。
> 生成时间：2026-09-24 21:29:07　实现：`新`　标注集：`/home/xgl/python/网盘管理_V2/数据/识别标注.jsonl`（91 条）

## 一、为什么先造尺子

架构文档（`docs/识别/00_架构.md`）第二节把话说死了：**没有标注集就不许说"更准了"**。真机上踩到的 `146.SDR.8bit…mp4`（标题被画质标签污染、季集全丢）只是个案，个案可以靠 `if` 蒙过去，但没人知道整体是进步还是退步。这张表就是"更准了"的定义：每次改动跑同一个脚本，数字只许升、不许降。

**口径**（读数字前先看，详见脚本文档字符串）：季的来源优先级 = 目录季 > 文件名季 > **默认 1**（「这是某一集 / 某几集的打包」但两处都没写季 → 期望 1，这是 Jellyfin 对没有季目录的剧的默认行为，也是真机仙逆 146/131 样本确认过的值；整季包/合集没有集号 → 期望 null；特别篇/NCOP/NCED → 第 0 季）；打包写法（`E01-E12`/`01-12`/`更新至12`/`全12集`）的期望 `集` = 1（区间上界是 `集到`，标注 schema 里没有这一列）；年份取「从后往前能留下非空标题」的那个，所以 `2012.mp4` 的期望年份是 null；标题比对前归一（大小写/空格/标点不敏感），中英混排的期望标题保留两种写法。

## 二、指标表

| 指标 | 命中 / 总数 | 准确率 | 门槛 | 达标 |
|---|---|---|---|---|
| 类型准确率 | 91 / 91 | 100.0% | — | — |
| 季准确率 | 91 / 91 | 100.0% | — | — |
| 集准确率 | 91 / 91 | 100.0% | — | — |
| 年份准确率 | 91 / 91 | 100.0% | — | — |
| **结构准确率**（季+集+年份 三项全对） | 91 / 91 | 100.0% | 99% | ✅ |
| 结构+类型（四项全对） | 91 / 91 | 100.0% | — | — |
| **标题清洗准确率** | 91 / 91 | 100.0% | 98% | ✅ |
| 全字段全对（标题+类型+季+集+年份） | 91 / 91 | 100.0% | — | — |
| 作品识别 TMDB Top-1 | — | 暂不可测 | 95% | 待 P2/P3 补标注 |
| 别名命中（中文 ↔ 英/日别别名） | — | 暂不可测 | 90% | 待 P4 别名库 |

**诊断**：标题里**残留画质/版本标签**（1080p/WEB-DL/x265/DDP5.1/中英双字…）的失败样例 0 条 —— 这类是 P1 归一化/清洗一次能修一片的。

> 指标是**快照**：标注集不动、只有代码变了的时候，两次数字才有可比性；
> 改了标注集就必须重跑一遍当新基线（数字降了就是回归，架构文档第六节第 4 条）。

> 标注集里已有 63 条带 `tmdb_id`；检索/打分接上之后就能算 Top-1（本脚本还未比对这一列）。

## 三、失败样例清单

（本次没有失败样例）

## 四、失败按写法粗分（参考）

> 这层分类是为了决定 P1 先修哪一类（纯启发式，可能分错；判定只看上一节）。

| 写法 | 失败 / 总数 | 失败率 |
|---|---|---|
| 集号在前（裸数字） | 0 / 20 | 0.0% |
| S01E01 家族 | 0 / 16 | 0.0% |
| 中文集话（第N集/话/回） | 0 / 14 | 0.0% |
| 英文标题（电影/剧集） | 0 / 2 | 0.0% |
| 打包/更新/合集/完结 | 0 / 8 | 0.0% |
| 中文标题（电影/剧集） | 0 / 4 | 0.0% |
| 1x03 / 03x05 | 0 / 2 | 0.0% |
| 字幕组方括号 | 0 / 7 | 0.0% |
| 特别篇/NCOP/NCED | 0 / 9 | 0.0% |
| 年份防误判 | 0 / 5 | 0.0% |
| 中英混排 | 0 / 4 | 0.0% |

---

重跑：`运行环境/venv/bin/python 工具/识别评测.py`（报告写到 `数据/识别评测_新管线.md`）
