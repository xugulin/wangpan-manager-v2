# 网盘管理 V2（自研播放内核）

**和 V1（`../网盘管理`）完全独立**：不 import V1 的任何模块、不读 V1 的配置与数据、
不调用 V1 的适配器/桥，也不依赖 V1 的 Python 环境（V2 有自己的 `运行环境/venv`）。

V2 的核心主张是用户定的路线：**自己用 libav\* 写播放器**，而不是把窗口交给现成播放器。

## 里程碑现状（M1–M6）

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 自研内核 | 解封装/解码/缩放/重采样 + 音频主时钟 + 自绘控件 + 播放/暂停/跳转/音量/截图/统计 | ✅ |
| M2 硬解 | VAAPI / D3D11VA + 按显示尺寸缩放；失败自动回退软解 | ✅ |
| M3 网络播放 | HTTP(S) Range、自定义头（UA/Referer/Cookie）、重连、打开失败降级、中断回调 | ✅ |
| M4 字幕 | SRT/ASS 解析 + 自绘叠加（黑描边/位置/对齐/颜色）+ 自动找同名字幕 + 轨道轮换 | ✅ |
| M5 体验 | 倍速、逐帧、播放记录与续播、播放列表、**章节跳转**、**进度缩略图预览**、键盘快捷键 | ✅ |
| M6 网盘功能 | 适配器契约 + 本地/HTTP 适配器 + 多任务传输引擎 + **多段并发下载** + **上传续传** + 网盘页 + **光鸭云盘真实适配器（真机验证过直链）** | ✅ |

### 现在能跑什么

* **自研解封装 / 解码 / 缩放 / 重采样**：ctypes 直连 `libavformat` / `libavcodec` /
  `libavutil` / `libswscale` / `libswresample`，**不调 ffmpeg 命令行、不用第三方 Python 绑定**；
* **音画同步**：音频写设备（PulseAudio）当时钟，视频按帧时间戳等待/丢帧；
* **自己的渲染**：帧 → `QImage` → 控件自绘（保持比例、居中、黑边），
  所以"视频跑到别的窗口里"这种事**结构上不可能发生**；
* 播放 / 暂停 / 停止 / 跳转 / 音量 / 截图 / 键盘（空格、←→、双击全屏、Esc）；
* 统计：已解码帧、丢帧、已播音频秒、码率、轨道摘要（状态栏实时显示）。

真机实测（作者的 KDE/COSMIC + XWayland + Intel iHD，本机 FFmpeg 63/61/10/7）：

```
网盘管理 V2 2.0.0｜FFmpeg：avformat 63.1.101、avcodec 63.1.101、
                          avutil 61.1.101、swscale 10.1.101、swresample 7.1.101
[播放] 已打开：视频#0 h264 640x360 25fps、音频#1 aac 44100Hz 1声道｜时长 4.0s
播放 2 秒：播放中｜1.9/4.0s｜25fps｜丢帧 0/50｜PulseAudio（libpulse-simple）
跳转 3.2s → 暂停（0 新帧）→ 继续 → 播完（结束=True）→ 截图 PNG ✓
```

## 怎么跑

```bash
./启动.sh                       # 空窗口，点「📂 打开文件」
运行环境/venv/bin/python 启动.py 某个视频.mp4
QT_QPA_PLATFORM=offscreen 运行环境/venv/bin/python -m unittest discover -s tests -t .
```

依赖：**Python 3.11+、PySide6（V2 自己的 venv 里）、系统 libav\***（Linux 装 `ffmpeg` 包即可；
Windows 把 `avformat-*.dll` 等放进 `运行环境/libav/`）。测试造素材用系统 `ffmpeg` 命令，
**播放本身不需要它**。

### 主要验证数据（本机真机，4K60 HEVC）

| 场景 | 结果 |
|---|---|
| 4K60 硬解播放（按显示尺寸输出 1280x544） | 300 帧 / 0 丢帧 / CPU ≈0.95 核（软解约 4~5 核） |
| 4K60 走本地 HTTP 播放（带 Referer/Cookie） | 300 帧 / 0 丢帧 / CPU ≈0.5 核 / Range 请求 3 次 / 停止 0.19s |
| 字幕 | 真机截图确认：`\an8` 顶部、`\c&H0000FF&` 显示为红色 |
| 章节 + 缩略图预览 | 真机截图确认：章节下拉列出三章、进度条上方出现解码缩略图 |


## 🆕 弹幕引擎 + TMDB 刮削 + 海报墙（三块都从零实现）

> 只参考公开协议与算法思想，**没有使用任何其它项目的代码/文件**。

| 模块 | 内容 | 验证 |
|---|---|---|
| **弹幕引擎** `wangpan/danmaku/` | 数据模型（不可变弹幕/按时间二分的弹幕池）；轨道分配（**位置=帧时间纯函数**、"间隙单调 ⇒ 两端满足即不追尾"的 O(1) 判据、长弹幕按 `(宽/基准)^0.28` 提速+确定性抖动）；渲染（**每条预渲染成带描边位图**再逐帧贴图、LRU 双上限、帧时间 PLL 平滑）；源（**Animeko 公益接口 = 零凭据可用**、弹弹play 官方签名/16MB MD5/`p` 串解析、本地 B站 XML 与我们自己的 JSON）；匹配（相似度打分 + 自动采纳阈值 + 人工确认）；过滤（词/正则/发送者/模式/彩色/时间窗/长度/去重 + 命中原因）；逐集记忆 | 单测 **138 条**；**真机**：视频上真的飘着弹幕（在线 79 条），且**在 GUI 里把「二刷」加进屏蔽词后 79 → 73 条、清空后回到 79**（`工具/真机_弹幕过滤.py`） |
| **TMDB 刮削** `wangpan/scrape/` | 命名解析（官方规范全覆盖 + 研究里 5 个坑的回归）；目录扫描（多版本/多段/番外归并、系统目录跳过）；TMDB 客户端（Bearer/api_key、语言回退、`include_image_language=zh,null`、429 退避、**缓存 180 天硬过期**符合官方条款、aggregate_credits、**导演精确匹配**、分级 CN→HK→TW→US 回退、**缓存按端点分区**、凭据杂字符清理）；**标准库 SOCKS5/HTTP 代理**（`代理.py`：直连不通时走本机 v2rayN）；**季集对齐**（官方 `/tv/{id}` 只给集数，逐季取集 + 本地兜底，保证集文件挂得上）；匹配打分（精确一致直接采纳、同名不同年交人工）；**待确认队列**（拿不准的**不入库**，候选存进任务表等人点）；**SQLite 11 张表**（含"无 TMDB id 的本地条目按路径各自成行"的去重身份）；图片分级尺寸缓存（LRU+原子写）；NFO 读写；刮削流水线（本地优先、分批、可取消、失败重试） | 单测 **116 条**（含 **16 条真 HTTP**：环回假 TMDB + 环回假 SOCKS5/HTTP 代理上跑真 urllib/真鉴权/真图片 CDN）；**真机对真 TMDB API**：`工具/真机_TMDB真API.py` —— 搜索/详情/分级/演职员/图片全真，《沙丘》(tmdb 438631, imdb tt1160419, HK:IIA/TW:6+/US:PG-13, 导演 丹尼斯·维伦纽瓦)、43 张真图落盘、集文件挂上（见下） |
| **海报墙** `wangpan/ui/海报墙页.py`、`详情页.py` | QListView + 自定义 Model/Delegate 虚拟滚动、分页、缩略图工作线程懒加载（去重排队 + LRU）、继续观看、搜索/筛选/排序、右键菜单、空状态；详情页背景+海报叠层、评分/影评人评分分开、分级徽章、标签、演职员（异步头像）、季集列表可点播 | 单测 16 条 + **真机截图**：采纳后新条目与真下下来的海报出现在墙上，工具条「✅ 待确认 N」计数随队列变化 |
| **待确认队列** `wangpan/ui/待确认.py` | 左边队列、右边候选（分数/标题/年份/类型/打分理由）；采纳=按选中的 TMDB id 直接刮（不再搜）；「换个名字再搜」接手动匹配；「忽略这条/全部」只写本地状态、不联网；采纳完**自动接着开下一条** | 单测 20 条 + **真机截图**（`/tmp/真机_待确认队列_对话框.png`、`..._采纳后海报墙.png`） |

### 自动匹配"拿不准"时的约定（这一版的核心取舍）

分数不够高 / 领先第二名不够多 / 压根没搜到 → 记进「刮削任务」表（状态 `需要确认`，
**候选列表一起存下来**），**不写资料库**。理由很实际：没有 TMDB 身份的条目在海报墙上
就是一张"标题=文件名"的空卡；而且 `UNIQUE(来源, tmdb_id)` 会把空 id 也当成一个值，
历史上的老库因此出现"第二部没刮到的片子把第一部顶掉"。现在的做法：

* 去重身份 = `tmdb:<id>`，没有 id 时退化成 `path:<哈希>`（**部分唯一索引**，空身份可多行）；
* 老库（带那条唯一约束的）在打开时**自动重建媒体表**（`foreign_keys=OFF` + id 原样搬，
  子表数据一行不丢，有迁移测试盯着）；
* 人在队列里点一下 → 走 `刮削服务.采纳候选()` 强制 ID 刮一次 → 那时才入库。


### 真 TMDB API 验证（本机网络直连不通 → 走本机 SOCKS5）

这台机器 **直连到不了 `api.themoviedb.org`**（DNS 被解析到无关 IP：`128.242.240.20` /
`2a03:2880:f127:83:face:b00c:0:25de`，IPv4/IPv6 全超时），而机器上跑着的
**v2rayN/xray 的本地 SOCKS5 口（127.0.0.1:10808）是通的**。所以 V2 自己实现了
标准库版 SOCKS5 / HTTP CONNECT 隧道（`wangpan/scrape/代理.py`，**不引第三方依赖**）：

```
数据/刮削.json → {"tmdb": {"proxy": "socks5://127.0.0.1:10808", …}}
或环境变量        V2_TMDB_PROXY=socks5://127.0.0.1:10808
```

`工具/检查刮削凭据.py` 会把**直连**与**经代理**两条路都测一遍（本机实测：
直连 API ❌ / 图片 CDN ❌；经代理 API HTTP 401 ✅（=连上了）/ 图片 CDN HTTP 200 ✅）。

真机对**真 API**跑出来的结果（`工具/真机_TMDB真API.py --开界面`，截图见下）：

```
配置摘要：Bearer｜socks5://127.0.0.1:10808｜zh-CN｜图片语言 zh,null｜TTL 7 天
[真 API] 搜索「沙丘 2021」→ 6 个候选；沙丘(2021)；沙丘(1984)；沙丘虫暴(2021)；沙丘行星(2021)
[真 API] 详情：《沙丘》2021｜评分 7.784｜时长 156 分钟｜状态 Released
         外链 {'tmdb': '438631', 'imdb': 'tt1160419'}｜分级 HK:IIA，TW:6+，US:PG-13，JP:G，AE:18TC
         导演 ['丹尼斯·维伦纽瓦']｜演员前 5 ['提莫西·查拉梅', '丽贝卡·弗格森', '奥斯卡·伊萨克', '杰森·莫玛', '斯特兰·斯卡斯加德']
         标签 ['科幻', '冒险']｜制片 ['Legendary Pictures']｜图片 193 张（含多语言）
[真图片] 海报下到本地：106219 字节
[真 API] 剧集：《怪奇物语》2016｜评分 8.551｜状态 Ended｜季 [(1, 8), (2, 9), (3, 8)]
[真 API] 第 1 季 8 集｜第 1 集《威尔·拜尔斯失踪》（2016-07-15）
[服务] 沙丘（2021）→ 100.0 分自动采纳（"标题与年份都对得上"）
[服务] 季集对齐：1 季 / 8 集（本地文件与在线季集详情已对上）
[落库] id=1｜沙丘（2021）｜movie｜7.8 分｜tmdb=438631｜导演 ['丹尼斯·维伦纽瓦']｜海报 85425 字节
        文件 2 个：沙丘 (2021) 1080p.mkv；沙丘 (2021) 2160p.mkv
[落库] id=2｜怪奇物语（2016）｜tv｜8.6 分｜tmdb=66732｜导演 ['罗斯·达菲', '马特·达菲']｜海报 81190 字节
        文件 2 个：怪奇物语 S01E01.mkv；怪奇物语 S01E02.mkv
[缓存] 图片 43 张｜1.2 MB
界面：海报墙两张真海报（8409/7403 种颜色的真照片，不是占位色块）、
      详情页真简介/真演职员头像/分级徽章 HK:IIA
```

**真 API 一轮就抓出 4 个只有真数据才会暴露的 bug**（都已修 + 有回归测试）：

| 现象 | 根因 | 修法 |
|---|---|---|
| 详情页"导演"是**副导演**（Borbála Debreczeni…） | `job` 按子串匹配 `director`，于是 First/Second Assistant Director、Casting Director、Art Director 全算导演 | 导演**精确匹配**（Director / Co-Director / Series Director），其余含关键词但非创作岗的职务排除 |
| 剧集入库了、海报也下了，但**一集文件都没挂上** | 官方 `/tv/{id}` 的 `seasons[]` **只有集数、没有集列表**，流水线没取 `/tv/{id}/season/{n}` → 条目里一个"集"都没有 | 新增"季集对齐"：只对本**地有文件的季**逐季取集（省请求），在线没有的集用本地文件名兜底建壳 |
| 「沙丘 (2021)」被塞进待确认队列 | 次佳《沙丘行星》(2021) 靠"片名包含 + 同年"拿到 94.2 分，与满分只差 5.8 < 12 | 新增规则：**片名精确一致 + 年份对得上 + 只有一个候选满足 → 直接采纳**；反过来"有同名但年份不符的候选、最佳只是名字像"则交人工 |
| 退出时 `QThread: Destroyed while thread is still running` → 进程核心转储 | 脚本 `QApplication.quit()` 不走 `closeEvent`；详情页的读图线程还活着 | 收尾抽成 `主窗口.退出前收尾()` 并挂到 `aboutToQuit`（停刮削线程/海报墙/详情页、关库、关窗），并有"退出会等线程"的回归测试 |

### 需要你自己提供凭据的两处（代码已就绪，只差 key）

* **弹幕**：弹弹play 现在**连 `/api/v2/match` 都要求 AppId 签名**（实测 HTTP 403
  `Missing Authentication Headers`）。到 <https://dev.dandanplay.com> 申请后写进
  `数据/弹幕源.json`（`{"dandanplay": {"appId": "…", "appSecret": "…"}}`）或设环境变量
  `V2_DANDANPLAY_APP_ID` / `V2_DANDANPLAY_APP_SECRET`。
  **默认根本不需要它**：源链是 **Animeko 公益接口（零凭据）→ 弹弹play → 本地同名文件**，
  真机上就是用 Animeko 拉到 79 条在线弹幕的。
* **刮削**：TMDB 的免费 Key，到 <https://www.themoviedb.org/settings/api> 申请后写进
  `数据/刮削.json`（`{"tmdb": {"token": "…"}}`）或设 `V2_TMDB_TOKEN`。
  **没配置也能用**：扫描 + 本地 NFO/图片照样入库（验证过）。
  本机直连到不了 `api.themoviedb.org`（域名被解析到无关 IP），**已经配好了走本机 SOCKS5**：
  `数据/刮削.json` 里 `{"tmdb": {"proxy": "socks5://127.0.0.1:10808"}}`（v2rayN 的默认 SOCKS 口）。
  换机器/换端口时改这个值，或设 `V2_TMDB_PROXY`；也可以用标准 `https_proxy`（HTTP 代理）。
  离线时"刮削链路"仍可全量验证：`tests/假TMDB服务.py`（环回口上的**真 HTTP** 假 TMDB，
  另有假 SOCKS5/HTTP 代理）。

### 数据与缓存位置

```
数据/资料库.db           SQLite（11 张表：媒体/文件/季/集/人物/媒体人物/图片/分级/标签/播放进度/刮削任务）
数据/缓存/图片/          海报等图片（分级尺寸 + LRU，默认上限 2GB，遵守 TMDB 不得缓存超 6 个月）
数据/缓存/tmdb/          TMDB 接口响应缓存（默认 TTL 7 天 + 180 天硬过期；按官方/代理端点分区）
数据/弹幕源.json         弹弹play 凭据（可选）
数据/弹幕设置.json       弹幕观感 + 过滤规则（界面里改完立刻落盘，下次启动自动装回）
数据/弹幕记忆.json       每个文件上次匹配到的剧集（下次打开自动装弹幕）
数据/刮削.json           TMDB 凭据与语言（可选）
```


## 真机验证（两台机器上是两套证据）

**Linux（开发机，FFmpeg 63 / VAAPI / PulseAudio）**

```
4K60 HEVC 硬解播放：300 帧 / 0 丢帧 / CPU ≈0.95 核（软解约 4~5 核）
4K60 走本地 HTTP（带 Referer/Cookie）：300 帧 / 0 丢帧 / CPU ≈0.5 核 / 停止 0.19s
字幕：\an8 顶部、\c&H0000FF& 红色（截图确认）
章节 + 缩略图预览：章节下拉三章、进度条上方出解码缩略图（截图确认）
光鸭云盘：列出真实根目录 34 项 → 取到真实签名直链 → Range 取 64KB 得 HTTP 206
          + Content-Range: bytes 0-65535/25580674693 + 前 8 字节 52617221（"Rar!"）
在线弹幕：Animeko 公益接口 → 79 条真弹幕（截图里真的飘在画面上）
弹幕过滤：在 GUI 面板里把「二刷」加进屏蔽词 → 79 → 73 条，清空后回到 79（控件路径生效）
待确认队列：3 部同名片 → 2 条进队列（候选 100.0 / 99.0 分，只差 1 分）→
            采纳「沙丘 2021」(tmdb:202) → 落库、海报下到本地（6645 字节）、文件挂上
状态栏：点「✅ 待确认 1」→ 关掉队列后左边显示「待确认队列：稍后再处理」，
        右边常驻显示播放统计（修掉了"统计每 16ms 一次把提示全盖掉"）
刮削链路（真 HTTP）：环回假 TMDB 收到 6 个请求 → 搜索/详情/分级/演职员/图片全部按官方形状解析
```

**Windows（GitHub Actions 真机 runner，FFmpeg 63，无 GPU）** ——
仓库：<https://github.com/xugulin/wangpan-manager-v2>，工作流 `测试（Windows 真机）`

```
偏移表主版本：63｜运行时 avcodec 主版本：63       ← 大版本一致性自检
单元测试：Ran 598 tests … OK (skipped=1)
真机验收（工具/windows真机验收.py）：结论 通过
  【本地文件】帧数 20、有帧 True、尺寸 (320,180)、截图 True、暂停有效 True、跳转生效
  【网络】    帧数 20、有帧 True、尺寸 (320,180)、截图 True、暂停有效 True、跳转生效
播放诊断（工具/诊断播放.py）：结论 出画面正常
硬解：D3D11VA 在无 GPU 的 runner 上建不起来 → **自动回退软解并如实记录**
      （这正是 M2 要求的"失败回退软解"，回退路径已在真 Windows 上验证；
        真正用上 D3D11VA 需要有 GPU 的 Windows 机器）
```

### 真机验证脚本（可重复跑，不需要人守着）

| 脚本 | 验什么 |
|---|---|
| `工具/真机_待确认队列.py` | 起环回假 TMDB（真 HTTP）→ GUI 里扫目录 → 待确认队列弹出来 → 采纳候选 → 落库/下图/挂文件，全程截图（跑完自动还原 `数据/资料库.db`） |
| `工具/真机_弹幕过滤.py` | 打开真视频 → 装在线弹幕 → 在设置面板里改屏蔽词 → 核对条数变化 + 截图（含时间轴偏移把最密的一段挪到片头） |
| `工具/真机_TMDB真API.py` | **对真 TMDB API** 跑搜索/详情/分级/演职员/真下图/落库，并可开真窗口截海报墙与详情页（跑完还原资料库） |
| `工具/检查刮削凭据.py` | 分别报 TMDB 的 **API 域名**与**图片 CDN** 的**直连**与**经代理**连通性，并列出当前凭据来源（不回显密钥） |
| `tests/假TMDB服务.py` | 环回口上的假 TMDB：真 HTTP/真鉴权/真图片 CDN，单测与真机脚本共用 |

## 目录

```
启动.py / 启动.sh          入口（自举到自带 venv）
wangpan/ffmpeg/            绑定：加载.py（找库）、绑定.py（原型 + 偏移读写）、偏移.py（自动生成）
wangpan/player/            播放内核：解封装.py、解码.py、音频输出.py、引擎.py
wangpan/subtitle/          字幕：模型.py、解析.py（SRT/ASS）、绘制.py
wangpan/danmaku/           弹幕：模型.py、轨道.py、渲染.py、引擎.py、过滤.py、匹配.py、记忆.py
                           + 源/（接口.py、animeko.py、弹弹play.py、本地.py）
wangpan/scrape/            刮削：命名解析.py、扫描.py、tmdb.py、匹配打分.py、库.py（SQLite）、
                           图片.py、NFO.py、服务.py（流水线 + 季集对齐 + 待确认队列）、
                           代理.py（标准库 SOCKS5/HTTP CONNECT 隧道）
wangpan/pan/               网盘适配器：接口.py（契约）、本地.py、http.py、工厂.py
wangpan/transfer/          传输引擎：任务.py、引擎.py（并发/暂停/取消/续传）
wangpan/ui/                界面：视频控件.py（自绘）、主窗口.py、网盘页.py、海报墙页.py、
                           详情页.py、手动匹配.py、手动资料.py、弹幕设置.py、待确认.py
tools/生成偏移.py          用 C 编译器的 offsetof 生成字段偏移（FFmpeg 换版本只需重跑它）
tools/生成素材.py          造测试素材
tests/                     绑定/解码/硬解/引擎/网络/字幕/体验/网盘传输/独立性/弹幕引擎/弹幕源/
                           弹幕过滤匹配/弹幕记忆/弹幕设置/TMDB客户端/TMDB真HTTP/命名解析/匹配打分/
                           刮削流水线/待确认队列/海报墙/手动资料/状态栏（598 条）
tests/假TMDB服务.py        环回口上的假 TMDB（真 HTTP，单测与真机脚本共用）
docs/研究/                  Animeko 弹幕、Jellyfin/Emby/Vidhub 播放与刮削的调研（只参考，不用其代码）
docs/架构与路线图.md        设计说明与后续里程碑
```

## 为什么不用现成播放器（V1 的教训）

V1 把窗口句柄交给 libvlc 让它画，于是踩了三类平台坑：`set_xwindow` vs `set_hwnd`、
输出模块（`xcb_x11` vs `direct3d11`）钉错平台、窗口"就绪"判定在 Windows 上失效，
还有"它自己开一个顶层窗口放画面"这种结构性问题。
V2 把画面数据握在自己手里，**这一类问题从设计上消失**。

## 许可

* 本项目代码：与 V1 相同的口径（源码公开、免费使用、自带免责声明）。
* FFmpeg：**LGPLv2.1+**（动态链接、不启用 GPL 组件）；随包分发时按 FFmpeg 官方清单
  提供来源与说明（<https://ffmpeg.org/legal.html>）。
* H.264/HEVC 专利提示：与 V1 用 VLC 时同样存在（FFmpeg 官方 legal 页有说明）。
