# ============================================================================
#  运行时产物与用户数据不进版本库
# ============================================================================
#  它们是"绿色版压缩包"的一部分（运行环境），或者本来就是用户自己的东西
#  （数据 / 配置：资料库、敏感词库、AI 学习库、缓存、截图、日志、登录令牌、API 密钥）。
# ============================================================================

# 自带运行环境：venv（几百 MB）、内置 ollama 基座、语音识别环境与模型
运行环境/

# 用户数据。
#   ⚠️ 这里**故意不写前导斜杠** —— `数据/` 会匹配**任意层级**下的 数据/ 目录，
#   于是三家适配器项目自己的 `适配器/*/数据/`（里面有登录令牌！）也一并被排除。
数据/

# 用户配置：里面有 API 密钥、网盘路径等，按 V1 的惯例不入库。
# 结构见 配置.example.json；文件缺失时程序直接用内置默认值，不会起不来。
配置.json

# Python 缓存
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# 构建产物
构建/发布/
构建/windows/

# 编辑器
.idea/
.vscode/
*.swp
