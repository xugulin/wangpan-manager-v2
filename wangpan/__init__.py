"""网盘管理 V2 —— 自研播放内核（libav* + Qt），完全独立于 V1。

设计基调
========
* **播放内核自己写**：拆包 / 解码 / 缩放 / 重采样 / 音画同步 / 渲染全部由
  :mod:`wangpan.player` 驱动，FFmpeg 只当库用（不调 ffmpeg 命令行、不用第三方绑定）；
* **界面自己画**：视频帧由 Qt 控件自绘（:mod:`wangpan.ui.视频控件`），
  不存在"把窗口交给第三方播放器、它自己开一个窗口"这类问题；
* **不依赖任何其它项目**：本包只 import 标准库、PySide6、以及系统/自带的 libav*。
"""

__version__ = "2.0.0"
